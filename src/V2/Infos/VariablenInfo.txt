! alle Variablen die Verändert werden können werden in anpassen.cpp
definiert!

MAIN: 

const d_speed = x; //durschnittsgeschwindigkeit beider Motoren
float l_speedfaktor = x; //faktor mit dem der d_speed Multipliziert wird
float r_speedfaktor = x; //faktor mit dem der d_speed Multipliziert wird
                            --> um Motorungleichheiten auszugleichen



IRMOVE:

long zeit;
long zeitA;
long zeitN;
long zeitH; 
long zeitL; 
long messungen; //durchgeführte Messungen
long highs; //Anzahl der highs
long lows; //Anzahl der Lows
long wechsel; //Wechsel zwischen HIGH und LOW
bool zustand; //HIGH oder LOW des IR-Sensors
float powerY; //Durschnittlicher Wert nach vorne/hinten berechnet aus den irWerten und den Constanten
float powerX; //Durschnittlicher Wert nach links/rechts berechnet aus den irWerten und den Constanten

float irValue[NUM_SENSORS] {}; // Array für 12 Sensoren
const float irXcon[NUM_SENSORS] {}; //Constanten zur Berechnung des power werts
const float irYcon[NUM_SENSORS] {}; //Constanten zur Berechnung des power werts



TORMOVE:

